<h1><%= @patient.name %></h1>
<% @patient.appointments.each do |appointment| %>
  <p>Appointment: <%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> - Doctor: <%= link_to appointment.patient_name, patient_path(appointment.patient) %></p>
<% end %>
